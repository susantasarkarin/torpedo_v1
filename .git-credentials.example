# Git Credentials Configuration
# Copy this file to .git-credentials and fill in your credentials
# DO NOT commit .git-credentials to git!

# GitHub Personal Access Token
GIT_USERNAME=GIT_USERNAME
GIT_PASSWORD=ghp_YOUR_TOKEN_HERE

# To use: source .git-credentials before running deploy.sh
# Or set these as environment variables in your systemd service

