[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    ai: mark tests that call AI/LLM endpoints (may be slow or cost money)
    readonly: mark tests that never write data (safe against production)
    smoke: mark tests as smoke/integration tests requiring a running server
