[Service]
# See torpedo-linkedin-worker.service.d/10-mongo-env.conf — same fix for the beat
# scheduler: the base unit's /etc/torpedo/linkedin-automation.env is empty, so point
# at the real env file to supply MONGO_URI and avoid a crash-loop.
EnvironmentFile=-/var/www/campaign_platform/backend/.env
